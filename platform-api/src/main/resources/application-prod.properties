# Production Profile Configuration
# PostgreSQL Database

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/platformdb}
spring.datasource.username=${DATABASE_USERNAME:platform}
spring.datasource.password=${DATABASE_PASSWORD:platform}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# JPA Configuration
# Note: Hibernate automatically detects PostgreSQLDialect, but explicit setting is fine for clarity
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
# Disable Open Session In View to avoid warnings and improve performance
spring.jpa.open-in-view=false

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=false
